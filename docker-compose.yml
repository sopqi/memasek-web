version: '3.8'

services:
  web:
    build: .
    restart: always
    ports:
      - "80:8000"
    volumes:
      - ./db.sqlite3:/app/db.sqlite3
      - ./media:/app/media
    environment:
      - DEBUG=False
      - SECRET_KEY=super_secret_key_memasek_2026
      - ALLOWED_HOSTS=*